export const API_URL = `${process.env.API_ORIGIN}/api/weblarek`;
export const CDN_URL = `${process.env.API_ORIGIN}/content/weblarek`;

export const settings = {
  templateCard: 'card-catalog', // template id - card-catalog 
  templatePrev: 'card-preview', // template id - card-preview
  templateBask: 'card-basket',  // template id - card-basket
  basketTemplate: 'basket',     // template id - basket
  orderTemplate: 'order',       // template id - order
  contactsTemplate: 'contacts', // template id - contacts
  successTemplate: 'success',   // template id - success
  modalTemplate: 'modal'        // template id - modal
};